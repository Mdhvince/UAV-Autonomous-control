[DEFAULT]
frequency = 10

# The MuJoCo scene owns gravity, timestep, vehicle characteristics and mission geometry.

[SIM_FLIGHT]
velocity = 3.0
# minimum distance to consider a waypoint as reached
min_dist_target = .5
